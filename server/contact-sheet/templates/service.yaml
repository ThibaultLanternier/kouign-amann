apiVersion: v1
kind: Service
metadata:
  name: {{ include "contact-sheet.fullname" . }}
  labels:
    {{- include "contact-sheet.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contact-sheet.selectorLabels" . | nindent 4 }}
